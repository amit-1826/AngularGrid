import { AppfocusDirective } from './appfocus.directive';

describe('AppfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AppfocusDirective();
    expect(directive).toBeTruthy();
  });
});
