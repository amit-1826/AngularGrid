import { AppblackDirective } from './appblack.directive';

describe('AppblackDirective', () => {
  it('should create an instance', () => {
    const directive = new AppblackDirective();
    expect(directive).toBeTruthy();
  });
});
